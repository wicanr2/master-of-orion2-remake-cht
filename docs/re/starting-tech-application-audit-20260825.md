# 先進級開局科技應用估值稽核（2026-08-25）

## 證據身分

- 輸入：`Orion2.exe`，SHA-256 `7ae2ac2e5904ca330009af2827279d889906b0b9b7a8854c38eb707a56e955b5`
- 工具：IDA Pro 9.4／IDAPython，映像 `fd2-ida-authorized-local:latest`
- 位址基準：IDA 線性位址，DOS/4GW LE object #1；原始函式名與位址均保留
- 可重生匯出器：`tools/ida/audit_calc_tech_value.py`

## 已證實

1. `sub_FC845 @ 0xFC845..0xFD199` 是 655 條 IDA 指令，不是舊文件依攤平 asm 行數所稱的 985／986 行函式。
2. `sub_FD335 @ 0xFD335..0xFD5EE` 掃描科技應用索引 `1..211`，候選必須是可取得、主題小於 75 且主題狀態為 2；它直接對應用評分，再呼叫 `sub_FE96F` 做一次加權抽選。原版不是「先抽主題、再抽應用」。
3. 分數為 `科技價值 × 視野回合 ÷ 研究回合`；研究回合是主題成本除以每回合研究點、下限 1。視野從 15 開始，沒有候選時反覆乘 `3/2`。
4. 固定六項完成後，topic 4、tech `0x72`、topic `0x49` 乘 2，tech `0x33` 乘 5；零估值改為 1。
5. 人類玩家以 `[player+0x28] == 100` 進入人類分支，基礎值為 `difficulty²×25+50`；類別 `0x1A/0x1B/0x1D/0x1E/0x21/0x15` 再乘 4。
6. 共用後段包含科技等級基礎表、同類已知科技邊際修正、其他玩家類別上限、早期 category `0x12` 乘 2，以及單一剩餘應用的 `base×10` 回退。
7. `sub_FD219` 透過 `sub_FD199` 建立其他玩家的類別上限；其 category 對應已由呼叫立即數閉合，不再是舊文件所稱的未定相鄰全域。
8. `sub_589D6 @ 0x589D6` 依序寫入 `[player+0x28]`、`[player+0x205]`、`[player+0x206]`，候選數分別為 6、4、7；人類保留 `+0x28 = 100`，後兩欄仍會抽選。

## 2026-08-25 AI 補證

`sub_589D6` 的 raw 6／4／7 加權初始化、`sub_FC845` AI category／種族分支與
`sub_FD335` 難度二次門檻已閉合並接線，詳見
[`ai-starting-tech-profile-audit-20260825.md`](ai-starting-tech-profile-audit-20260825.md)。

## 強推論與未知

- `[player+0x205]` 的四值確實被 `sub_FC845` 後段消費，但四個 raw 值的玩家可見名稱尚未閉合；remake 不替它命名，也不猜人類開局值。
- raw 6／4／7 的對玩家名稱仍未閉合；實作保留 raw 值，不再回退到科技類別權重。
- `sub_FD335` 直接讀玩家 0 的 `[+0xAC]` 作研究點。這可能是呼叫情境造成的共用值，也可能是原版不對稱；在 caller 尚未完全閉合前不把它推廣成每位 AI 的精確規則。

## 推翻的舊斷言

- `docs/re/calc-tech-value.md` 是歷史逐段抄錄，當中的「985 行」、「weight 一律 1」、相鄰 best-category 全域未解，以及邊際公式舊讀法均已被本次 IDA 資料庫交叉參照與資料流推翻。
- 現行結論以本檔為準；舊檔只保留定位細節，不再作現況規格。
