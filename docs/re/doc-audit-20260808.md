# 文件稽核:斷言 vs 程式碼現況(2026-08-08)

> 稽核範圍:`docs/HONEST-STATUS.md`、`WORKLIST.md`、`docs/tech/*.md`(53 份)、`README.md`、`CLAUDE.md`。
> 方法:從文件抽可驗證斷言(具體數字/函式名/檔案存在性/「已做/未做」),逐條用 `grep`/`Read` 對照程式碼查證,只回報「文件說 A、程式碼是 B」的不符項。
> 不查 `docs/re/01-gap-report.md`(按時序施工日誌,設計上就會被後面條目推翻)。
> **本次稽核期間 `internal/shell/`、`internal/gamedata/`、`internal/engine/` 有另一個 session 同時在改叛亂系統**
> (`git status` 當時顯示 `internal/engine/engine.go`、`internal/gamedata/ground_battle_orig.go`、
> `internal/shell/assimilation.go`、`internal/shell/session.go`、`internal/shell/ground_invasion.go` 為
> modified,新增 `internal/shell/rebellion.go`)。凡涉及叛亂/同化系統的斷言,查證時已避開這批移動中的檔案,
> 只用已定案的部分(如 `GroundTypeRebels` 常數本身已存在且已改名)。

## 高:會讓人做錯決定(誤判已完成的工作為未完成,或反過來)

### 1. 網路多人對戰已實作,但 README.md 與 multiplayer-architecture.md 都還寫「未做」

- **文件**:`README.md:139` — 「連線多人對戰:**熱座(同機輪流)已可玩**,網路 / 數據機 / 序列埠直連未做(方向已定案為 lockstep over TCP…)」
- **文件**:`docs/tech/multiplayer-architecture.md:89-90` — 「**網路 / 數據機 / 序列埠直連未做**——設定畫面上那三顆是灰的…上表第 2 步(引擎決定性化)與第 3 步(lockstep over TCP)仍未開工。」
- **程式碼**:`internal/netplay/`(`lockstep.go`、`lobby.go`、`discovery.go`、`chat.go`、`protocol.go`、`frame.go`,均有對應 `_test.go`)已完整實作;`docs/HONEST-STATUS.md:101-112` 明確記載「網路對戰 2026-08-07 都已可玩」,並列出大廳配號/廣播種子/UDP 區網探索/6 張畫面/聊天列等細節。
- **時序證據**:`git log` 顯示 `multiplayer-architecture.md` 的 `db4fbb7`(2026-08-07 03:49)commit 訂正過一次「多人未接」的舊斷言,但 `internal/netplay` 的 5 個功能 commit(`fcf4914`/`a280844`/`3e78e21`/`57aecc3`/`2b61a09`)全部發生在同一天 20:12–23:11,**晚於**那次文件訂正。`README.md` 最後一次改動(`9847db1`,2026-08-07 19:24)同樣早於這批 netplay commit。兩份文件都在網路多人真正做完「之前」被鎖定,之後沒有回頭更新。
- **嚴重度**:高(兩份對外/技術文件同時聲稱一個已完成的核心功能不存在,容易導致重工或誤判專案範疇)
- **建議改法**:兩處都改成「熱座 + 網路對戰(lockstep over TCP)均已可玩,數據機/序列埠因硬體已消失明確不做」,並在 `multiplayer-architecture.md` §5 補一段 2026-08-07 20:00 之後的實作記錄。

### 2. 領袖技能:README 說「已接 2 項」,實際是 13 項

- **文件**:`README.md:122` — 「間諜(偷科技最小迴圈)、領袖技能(**25+ 技能中已接 2 項真實效果**)」;同樣措辭重複於 `README.md:320`。
- **程式碼/對照文件**:`docs/HONEST-STATUS.md:118-133` 記載「2026-08-07~08 第 101~104 項逐條查手冊後,共 **13 項技能有實際效果**」;`grep -rohE "SKILL_[A-Z_]+" internal/shell internal/engine`(排除測試檔)命中 15 個不同技能常數(`SKILL_COMMANDO`/`SKILL_ENGINEER`/`SKILL_ENVIRONMENTALIST`/`SKILL_FARMING_LEADER`/`SKILL_FINANCIAL_LEADER`/`SKILL_HELMSMAN`/`SKILL_INSTRUCTOR`/`SKILL_LABOR_LEADER`/`SKILL_MEDICINE`/`SKILL_NAVIGATOR`/`SKILL_RESEARCHER`/`SKILL_SCIENCE_LEADER`/`SKILL_SPIRITUAL_LEADER`/`SKILL_TRADER`/`SKILL_WEAPONRY`),與「2 項」數量級不符。`docs/tech/leader-officer-skills.md` 本身已在 2026-08-08 修訂為現況一致的說法。
- **嚴重度**:高(README 是外部讀者的第一印象文件,把已完成 13 項的系統寫成只做 2 項,嚴重低估完成度)
- **建議改法**:兩處改成「25+ 技能中已接 13 項真實效果(殖民地經濟 11 項 + 地面戰指揮官 + 領航員),其餘 captain/common 技能對應子系統尚未做」。

### 3. component-tech-mapping.md 整份文件的「16 個元件掛錯科技主題」結論已被修正,程式碼現在是對的

- **文件**:`docs/tech/component-tech-mapping.md:74` — 「**目前掛的 TOPIC 錯誤(TECH 有真實對應,但 TOPIC 指錯)**:16 個 —— 中子爆破槍、核融合光束、高斯砲、相位砲、電漿砲、佐特裝甲、中子素裝甲、精金裝甲…」(逐行列出「目前掛的 TOPIC」與「正確 TOPIC」兩欄,宣稱兩者不同)
- **程式碼**:`internal/shell/session.go` 逐一核對,例如:
  - 中子爆破槍(:201)已是 `gamedata.TOPIC_NEUTRINO_PHYSICS`(文件說的「正確 TOPIC」)
  - 核融合光束(:202)已是 `gamedata.TOPIC_FUSION_PHYSICS`
  - 高斯砲(:204)已是 `gamedata.TOPIC_SUBSPACE_FIELDS`
  - 相位砲(:205)已是 `gamedata.TOPIC_MULTIPHASED_PHYSICS`
  - 電漿砲(:206)已是 `gamedata.TOPIC_PLASMA_PHYSICS`
  - 佐特裝甲(:239)已是 `gamedata.TOPIC_NANO_TECHNOLOGY`
  - 中子素裝甲(:240)已是 `gamedata.TOPIC_MOLECULAR_MANIPULATION`
  - 精金裝甲(:241)已是 `gamedata.TOPIC_MOLECULAR_CONTROL`
  - 自動修復(:255)已是 `gamedata.TOPIC_ADVANCED_MANUFACTURING`
  - 隱形裝置(:256)已是 `gamedata.TOPIC_DISTORTION_FIELDS`

  逐一比對文件「正確 TOPIC」欄與程式碼現況,**全部吻合**——即文件指出的錯誤已經全部修正,但文件表格與「結論」段落沒有更新,仍宣稱這些是「目前掛的 TOPIC 錯誤」。
- **嚴重度**:高(若照這份文件的結論去「修正」程式碼,會把已經正確的對應改回錯的)
- **建議改法**:整份文件的「目前掛的 TOPIC」欄需要重新掃一次 `session.go` 現況,結論段落改成「先前 16 個錯誤已於某輪修正,現況見下表」。

### 4. colonization.md:「每星固定一顆行星、不生成氣態巨星/小行星帶、不能選擇殖民哪一顆」已被多星體改版推翻

- **文件**:`docs/tech/colonization.md:29-38` — 「MOO2 的行星分兩大類…**remake 目前每個星系固定生成一顆「一般行星」,從未生成氣態巨星或小行星帶**」;`:123` — 「目前每星固定一顆行星,不需要〔行星選擇子畫面〕」;`:134-136` — 「不支援『同系統多顆行星、選擇殖民哪一顆』…本 remake 每星固定一顆行星」。
- **程式碼**:`internal/shell/session.go`:
  - `:1961`、`:4219` — `order := []gamedata.PlanetType{gamedata.HABITABLE, gamedata.GAS_GIANT, gamedata.ASTEROIDS}`(明確會生成三種星體類型)
  - `:1890` — `types[o], _ = gamedata.RollSatelliteType(r.Intn(10)+1, o, r.Intn(100)+1)`(逐衛星擲骰決定類型)
  - `:1752` 註解「氣態巨星/小行星帶不能直接殖民」暗示三種星體皆已生成、只是可殖民性不同
  - `genPlanets`(`:1820`)本身依星系生成多顆行星(非固定一顆)
- **嚴重度**:高(這是「子系統尚未做」還是「子系統已做完」的架構級誤判,容易導致重工或誤導後續任務規劃)
- **建議改法**:重新核對 `genPlanets`/`RollSatelliteType`/`ColonizePlanet` 現況後,整份文件的 §1.1、§3 段落需要重寫,不是局部訂正。

## 中:誤導但不致命

### 5. README.md「3604 條字串」已被 5,046 條取代,未回頭更新

- **文件**:`README.md:21` — 「顯示層 i18n 覆蓋…**3604 條字串**、22 個 LBX 字串來源全數 account」;同樣數字重複於 `README.md:113`。
- **程式碼**:`awk -F'\t' '$1 !~ /^#/ && $1 != ""' assets/i18n/*.tsv | wc -l` 實測 **5046**,與 `docs/HONEST-STATUS.md:138`「譯表 5,046 條(24 份 TSV)」完全吻合。24 個 TSV 檔案數也對得上。
- **嚴重度**:中(是 README 開頭招牌數字,3604 是 2026-07-02 `string-sources.md` 定案時的舊快照,829 條隨機星名 + 672 條艦名等專有名詞池落地後從未回頭更新這個總數,連 2026-08-07 那次「README 換上最新畫面 + 修掉過期斷言」的 commit 都沒抓到)
- **建議改法**:改成「5,046 條字串」,並在旁註明查法(`awk` 濾註解列)避免下次又抓錯。
- **處置**:複查後 README.md:21、113 現況已是「5,046 條字串(24 份 TSV)」,已由使用者在本輪之前處理完成。README.md 屬本輪排除清單(使用者已處理),不再動作。

### 6. README.md「191 個單元測試全綠」大幅低估實際測試數

- **文件**:`README.md:22` — 「遊戲邏輯從零重建…對官方手冊 + openorion2 逐條驗證,**191 個單元測試全綠**(戰鬥命中/傷害、飛彈、間諜、地面戰、殖民成長、士氣、收入…)」
- **程式碼**:`grep -c "^func Test" internal/gamedata/*.go` = **315**(僅 `internal/gamedata` 一個套件,句子提到的系統全在這裡);`grep -rE "^func Test" internal cmd`(排除 `openorion2`)全專案合計 **1173**。
- **嚴重度**:中(數字本身不影響功能判斷,但明顯是早期快照,容易被引用成「測試覆蓋率就這樣」)
- **建議改法**:改成約略語氣「300+ 個 `gamedata` 單元測試」或直接引用即時 `grep` 指令,避免寫死數字。
- **處置**:複查後 README.md:22 現況已是「300+ 個 `gamedata` 單元測試全綠(全專案 1,100+;…)」,已由使用者在本輪之前處理完成,採用的正是本報告建議的近似語氣寫法。README.md 屬本輪排除清單,不再動作。附註:本輪複查當下重新實測(`grep -c "^func Test" internal/gamedata/*.go` 加總、`grep -rE "^func Test" internal cmd | grep -v openorion2 | wc -l`),數字為 `gamedata` 323、全專案 1190,比稽核報告記載的 315/1173 又增加,因稽核期間另一 session 同時在改叛亂系統、持續新增測試,印證「約略語氣」比寫死數字更耐用。

### 7. moo2-formulas-reference.md 自稱「14 個系統」,但兩份索引文件仍寫「8 個系統」

- **文件**:`docs/tech/README.md:11` — 「moo2-formulas-reference.md — …殖民地成長、生產/污染、研究樹、軍官、艦艇衍生值、光束命中、飛彈防禦、間諜**共 8 個系統**…」
- **文件**:`docs/tech/game-logic-port.md:42` — 「全部已驗證公式彙整於 `docs/tech/moo2-formulas-reference.md`(**8 系統 40 公式**,附來源)。」
- **程式碼/文件本身**:`docs/tech/moo2-formulas-reference.md` 開頭明寫「目前涵蓋 **14 個系統**」,目錄列出 1~14(殖民地人口成長、生產與污染、士氣、國庫收入、地形改造、研究樹、軍官、艦艇衍生值、艦艇戰鬥屬性推導、光束命中、光束傷害解算、飛彈防禦、地面戰、間諜)。兩份索引只列了其中 8 個,漏掉士氣/國庫收入/地形改造/艦艇戰鬥屬性推導/光束傷害解算/地面戰 6 個系統。
- **嚴重度**:中(索引文件本身沒有錯誤資訊,但明顯是文件擴充後索引沒同步更新,會讓人以為公式庫只涵蓋 8 個系統)
- **建議改法**:兩處改成「14 個系統」,並補齊系統清單。
- **處置**:已訂正。複查確認 `moo2-formulas-reference.md` 目錄實際是 `## 1.` 到 `## 14.`(殖民地人口成長/生產與污染/士氣/國庫收入/地形改造/研究樹/軍官/艦艇衍生值/艦艇戰鬥屬性推導/光束武器命中/光束傷害解算/飛彈防禦與反飛彈火箭/地面戰/間諜),`## 15.`、`## 16.` 是附錄不計入系統數。`docs/tech/README.md:11`、`docs/tech/game-logic-port.md:42` 都已改成「14 個系統」並用引註標記原句過期。

### 8. moo2-formulas-reference.md §16「尚未移植/待查證」表格,多列系統其實已經做完

- **文件**:`docs/tech/moo2-formulas-reference.md:1132` — 「外交 | 未見 | 連 `DiplomacyView` 畫面殼都不存在…」;`:1134` — 「回合編排(把上述公式串成回合) | 待補 | `researchProgress`/`experience` 等欄位全 repo 除建構子外從未被賦值,**無回合結算函式存在**」;`:1138` — 「勝利條件 | 未見 | 三種勝利路徑…全無」
- **程式碼**:`internal/diplomacy/relations.go` 有完整 17 級關係系統;`internal/engine/game.go:21`、`internal/engine/empire.go:42`、`internal/engine/colony.go:81` 分別是 `func RunGameTurn`、`func RunEmpireTurn`、`func RunColonyTurn`——回合結算函式確實存在;`internal/ai` 有 `Decider` 介面與 `original.go` 的難度加成表;三條勝利路徑見 `docs/tech/victory-conditions.md`(已核對函式存在)。
- **附註**:表格前言寫「以下系統 openorion2 完全沒有可複用的邏輯」,若解讀成「這是在描述 openorion2(唯讀參考碼),不是描述 remake」,則本身沒錯(openorion2 確實至今仍是唯讀殼)。但表格標題是「**尚未移植**/待查證」,且「回合編排」那一列用詞是「無回合結算函式存在」(不是「openorion2 沒有」),讀者很容易誤讀成 remake 現況。
- **嚴重度**:中(用詞歧義,容易誤讀成 remake 缺這些系統,但文件前言有但書可自辯)
- **建議改法**:表格加一欄「remake 現況」,或在表格前補一句「本表僅描述 openorion2 基底,不代表 remake 完成度,remake 現況見 HONEST-STATUS.md」。
- **處置**:已訂正。複查確認 `internal/engine/game.go` 有 `func RunGameTurn`、`internal/engine/empire.go` 有 `func RunEmpireTurn`、`internal/engine/colony.go` 有 `func RunColonyTurn`;`internal/diplomacy/relations.go` 有完整 17 級關係列舉 + `cmd/moo2/diploview.go` 外交畫面(`DiplomacyView` 字面確實不存在,但功能等價的畫面存在);`docs/tech/victory-conditions.md` 記載三條勝利路徑已全部接線(3/3)。表格前補了訂正說明,「外交」「回合編排」「勝利條件」三列各自加註「remake 現況」,未改動其餘列(那些列本來就只在描述 openorion2,沒有歧義)。

### 9. tactical-combat-assets.md「#12 艦級↔CMBTSHP 對照 RE-gated」結論已被同日稍晚的文件與程式碼推翻

- **文件**:`docs/tech/tactical-combat-assets.md`(2026-07-11)— 「結論:#12(艦級 → CMBTSHP 資產索引 完整對照)確實 RE-gated…現行「全艦共用 CMBTSHP#30 佔位」維持,待 DOSBox oracle 就緒再逐級對照」
- **程式碼/對照文件**:同一天 16:30 新增的 `docs/tech/cmbtshp-ship-sprites.md`(標題註明「免 RE/oracle」)與 `internal/shell/session.go` 的 `CombatSpriteForClass`/`CombatSpriteForStrength`(依艦級分色塊分配資產索引)、`cmd/moo2/interactive.go` 的 `loadCombatShipByIdx`,均顯示現行已是逐艦型索引,不是「全艦共用 #30」的佔位狀態。
- **嚴重度**:中(RE-gated 的結論已經過期,若照抄會誤判這條還卡在需要 DOSBox 才能解)
- **建議改法**:在文件結尾補一段指向 `cmbtshp-ship-sprites.md` 的訂正說明。
- **處置**:已訂正。複查確認 `docs/tech/cmbtshp-ship-sprites.md`(2026-07-11 16:30 新增,同日稍晚於本文件)存在,`internal/shell/session.go` 有 `CombatSpriteForClass`/`CombatSpriteForStrength`(依艦級/戰力分色塊選 sprite 索引),`cmd/moo2/interactive.go` 有 `loadCombatShipByIdx`,均已是逐艦型索引邏輯。在 `tactical-combat-assets.md` 結尾補了訂正段落指向 `cmbtshp-ship-sprites.md`。

### 10. colony-economy-maintenance.md 的 BC 崩潰下限常數值已變

- **文件**:`docs/tech/colony-economy-maintenance.md:267,288` — 「`bcCrashFloor80Turns=-20`/`bcCrashFloor300Turns=-150`」
- **程式碼**:`internal/shell/antares_test.go:20` — `const bcCrashFloor80Turns = -40`;`internal/shell/events_test.go:27` — `const bcCrashFloor300Turns = -400`。兩個常數都比文件記載的寬鬆一倍以上。
- **嚴重度**:中(門檻值本身只影響測試容許範圍,但文件記錄的具體數字已經不對)
- **建議改法**:更新成 -40 / -400,並註明是哪次改動放寬的。
- **處置**:已訂正。複查確認 `internal/shell/antares_test.go:20` 現值 `bcCrashFloor80Turns = -40`、`internal/shell/events_test.go:27` 現值 `bcCrashFloor300Turns = -400`,與文件記載的 -20/-150 不符。已在 `colony-economy-maintenance.md` §6.6 補訂正說明並附 `grep` 查法;是哪次改動放寬的未查證(commit 歷史未追,超出本輪範圍),文件中如實註明「未查證」。

### 11. README.md「仍缺」清單把「戰機/航母」寫成完全未做,實際已部分接進戰鬥解算

- **文件**:`README.md:136` — 「戰機/航母、完整 spy/leader/diplomacy UI、多 AI 對手的目標選擇策略…」(列在「仍在進行/仍缺」)
- **程式碼**:`internal/gamedata/combat.go:292,301` 的 `FighterBayCombatContribution`/`FighterHeavyBayCombatContribution` 已被 `internal/shell/session.go:606,610`、`internal/shell/repair.go:67,70`、`internal/gamedata/planet_defense.go:97,99` 呼叫,戰機已實質貢獻戰鬥力與 HP。`docs/HONEST-STATUS.md:80-85` 已釐清「戰機已經接進快速艦隊戰鬥了…真正缺的是戰術格子戰鬥裡的獨立戰機單位」。
- **嚴重度**:中(不是完全捏造,但用詞比實際情況悲觀,容易讓人重做已完成的部分)
- **建議改法**:改成「戰術格子戰鬥裡的獨立戰機單位(出擊/攔截/回收)——快速戰鬥的戰機力已接」。
- **處置**:複查屬實(README.md:136 現況仍是「戰機/航母、完整 spy/leader/diplomacy UI…」,`internal/gamedata/combat.go:292,301` 的 `FighterBayCombatContribution`/`FighterHeavyBayCombatContribution` 確實已被 `session.go:606,610`、`repair.go:67,70`、`planet_defense.go:97,99` 呼叫),但目標檔案是 `README.md`,屬本輪排除清單(使用者已處理其餘部分,此行未動)。依邊界指示不修改 README.md,本輪不動作,留給下一輪或使用者處理。

### 12. gameplay-systems-status.md 建築表進度標記停在「進行中:5→40 棟」

- **文件**:`docs/tech/gameplay-systems-status.md:99` — 「殖民地建築全表(進行中):5 棟 → 手冊 40 棟入 `gamedata/buildings.go`,綁前置科技 gating(subagent 實作中)。」
- **程式碼**:`internal/gamedata/buildings_test.go:17` — `if got := len(Buildings); got != 41`,`docs/HONEST-STATUS.md:62`「建築表 41 棟全部有程式碼消費(2026-08-07 第 97 項重掃確認,0 棟純擺設)」。
- **嚴重度**:中(進度標記早已完成,但文件仍停在「進行中」)
- **建議改法**:改成「✅ 完成(41 棟,見 HONEST-STATUS.md)」。
- **處置**:已訂正。複查確認 `internal/gamedata/buildings_test.go:17` 現況為 `if got := len(Buildings); got != 41`,`docs/HONEST-STATUS.md:64`「建築表 41 棟全部有程式碼消費」。已把 `gameplay-systems-status.md` 該行改成「✅ 完成(41 棟,見 `docs/HONEST-STATUS.md`)」,原句以引註保留並附查法。

## 低:用字過期,不影響判斷

### 13. HONEST-STATUS.md「部隊類型 3 未定名」已因常數改名而過期

- **文件**:`docs/HONEST-STATUS.md:61` — 「…`[player+0x8A7]` 與 `[+0x10]` 未定名、守方裝甲那格留 0、**部隊類型 3 未定名、類型 3 未定名**)…」
- **程式碼**:`internal/gamedata/ground_battle_orig.go:95` — `GroundTypeRebels = 3 // 叛軍(−20,最弱;見上方的定名依據)`;`internal/shell/rebellion.go:23,31` 記載「`gamedata.GroundTypeFourth` 一直掛著⚠未定名…常數已改名為 `GroundTypeRebels`」。部隊類型 3 已定名為「叛軍」。
- **嚴重度**:低(是同一句話裡兩次重複同一件事,現在都已解掉;此條為協作者於稽核期間主動告知的即時變更)
- **建議改法**:刪掉「部隊類型 3 未定名、類型 3 未定名」這兩個重複子句。
- **處置**:複查不成立。`docs/HONEST-STATUS.md:61` 現況已是「仍缺:`[player+0x8A7]` 未定名、守方裝甲那格留 0。**部隊類型 3 已於 2026-08-08 第 105 項定名為叛軍**」——重複子句已不存在,是稽核期間另一 session(叛亂系統重寫)同步訂正掉的,本輪不需再處理。

### 14. sprite-tile-quality.md「完全沒有內部放大或縮放邏輯」字面過期,核心結論仍成立

- **文件**:`docs/tech/sprite-tile-quality.md:17`(2026-07-03)— 「**目前完全沒有內部放大或縮放邏輯**(無 `GeoM.Scale`、無自訂 `Filter`…)」
- **程式碼**:`cmd/moo2/cutscene.go:126`(過場動畫依 `s.scale` 縮放,2026-08-07 加入)、`cmd/moo2/interactive.go:2112,2115`(戰術戰鬥艦艇 sprite 依格高等比縮小)、`internal/uifont/font.go:109,133`(點陣字 2× 整數放大 + `FilterNearest`,2026-07-11 隨 `pixel-font-decision.md` 加入)均已使用 `GeoM.Scale`。
- **嚴重度**:低(文件討論的核心主題是「畫布級/顯示級」整體放大,這部分至今確實沒做,只有局部 sprite/文字縮放,字面斷言過期但論點方向沒錯)
- **建議改法**:改成「目前局部 sprite/文字有縮放,但無全域畫布放大」。
- **處置**:已訂正。複查確認 `cmd/moo2/cutscene.go:71-76`(`s.scale`)、`cmd/moo2/interactive.go:2112,2115`、`internal/uifont/font.go:109,133`(`FilterNearest`)均已用 `GeoM.Scale`,但都是局部/單一元件縮放,`Layout()` 回傳值仍固定 640×480,沒有畫布級整體放大。已改用「目前沒有畫布級/全域放大或縮放邏輯」+ 訂正段落列出三處局部縮放,核心論點(無全域畫布放大)保留不變。

### 15. rules-implementation-audit.md 的 openorion2 行數估計與實測有出入

- **文件**:`docs/tech/rules-implementation-audit.md:9` — 「全 repo(`src/*.cpp` 約 16,600 行核心邏輯檔)」
- **程式碼**:`wc -l openorion2/src/*.cpp` 實測 **18712** 行,差約 13%。
- **嚴重度**:低(文件用「約」且未指明統計口徑,可能排除了特定檔案;不影響文件的核心結論——`STUB(view)` 22 處、`rand()/srand()` 零命中兩條關鍵斷言都逐一 grep 核對過,精確吻合)
- **建議改法**:若要保留精確數字,註明統計口徑(含哪些檔案);否則直接拿掉這個數字,只保留「STUB 22 處 / 零 RNG 命中」兩個已核實的斷言。
- **處置**:已訂正。複查確認 `wc -l openorion2/src/*.cpp` 實測 18,712 行,與文件的「約 16,600 行」差約 13%。選擇保留精確數字並註明實測值與差距(未深究是否排除了特定檔案),不影響下游的 `STUB 22 處`/`零 RNG 命中`兩條核心結論。

### 16. palette-chain.md 一處孤立的舊識別字殘留

- **文件**:`docs/tech/palette-chain.md:48` — 「已實作於 `cmd/moo2/interactive.go`:`paletteProvider` + `resolvePalette()`」
- **程式碼**:`cmd/moo2/interactive.go:248` — `type paletteChain []assetRef`;程式碼裡沒有任何 `paletteProvider` 識別字。同一份文件第 22 行其實已經正確使用 `paletteChain` 這個名字,第 48 行是孤立的殘留舊稱。
- **嚴重度**:低(單一行的型別名寫錯,不影響對整條調色盤鏈機制的理解)
- **建議改法**:把第 48 行的 `paletteProvider` 改成 `paletteChain`。
- **處置**:已訂正。複查確認 `cmd/moo2/interactive.go` 全檔無 `paletteProvider` 識別字,正確型別是 `interactive.go:248` 的 `type paletteChain []assetRef`。已把第 48 行改成「~~`paletteProvider`~~ `paletteChain`」並附訂正說明,原稱以刪節線保留痕跡。

## 未掃完的範圍

- **`WORKLIST.md`(1950 行)未完成逐行稽核**。原規劃派一個背景 subagent 全文掃描,但稽核過程中收到指示「不要再等背景工作」,故提前收斂,該 subagent 的結果未納入本報告。
  我本人直接讀過的部分只有檔案開頭 1-80 行(「★ 2026-08-08 現況盤點與剩餘工作清冊」段落)——這段本身內容與 `HONEST-STATUS.md` 一致(領袖技能 13 項、gameplay 子系統全接线),且文件開頭已自行聲明「底下 Phase 0–4 有不少 `[ ]` 其實早就做完了…要判斷『某項做了沒』,先 grep 程式碼,別信這裡的方框」,即文件自己承認 Phase 0-4 區塊不可信。**中段與後段(Phase 0-8 的逐輪紀錄、約 1870 行)完全沒有核對過**,不排除其中有可驗證的具體斷言(數字/函式名/檔案名)已經過期。
- **`docs/tech/moo2-formulas-reference.md`**(1154 行)只重點抽查了前言、目錄、§16 表格,§1-14 逐條公式本身(含 openorion2 行號引用)沒有逐一核對。
- **`docs/tech/screen-spec-info-research.md`、`docs/tech/tactical-combat-weapon-kinds.md`** 裡對 `openorion2/src/info.cpp`、`tech.cpp` 的具體行號引用只抽查了函式存在性,未逐行核對行號本身是否仍準確(唯讀參考碼,行號本身不影響 remake 正確性,判斷價值較低,故未深究)。
- **`docs/tech/ui-typography-button-review.md`** 裡引用的 `cmd/moo2/interactive.go` 具體行號(如 372-373、1443-1454)因為該檔案從審查當時的 2346 行成長到現在約 4483 行,行號已系統性位移;但抽查的實質內容(按鈕顏色/字級/`drawBarLabelsCHT` 現況)核對後仍然正確,判斷為「行號漂移」不是真正的斷言錯誤,未列入正式發現。
- 涉及叛亂/同化系統(`internal/shell/rebellion.go`、`internal/shell/assimilation.go`)的文件斷言,因稽核期間該區塊由另一個 session 同時在改,**刻意略過**,避免回報一個馬上就會被下一個 commit 推翻的「發現」。
- 需要實際跑遊戲才能驗證的斷言(如「60 回合玩家 BC 健康爬升 55→419」「70 回合無 panic」「-gamegallery 26 畫面端到端」)一律未查證——這類斷言只能靠重跑 headless 模擬器/截圖廊驗證,不在本次「讀檔 + grep」方法範圍內。
