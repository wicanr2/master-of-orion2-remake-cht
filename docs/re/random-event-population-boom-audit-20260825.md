# 隨機事件 17 人口暴增稽核（2026-08-25）

## 證據契約

- 輸入：`Orion2.exe`，SHA-256 `7ae2ac2e5904ca330009af2827279d889906b0b9b7a8854c38eb707a56e955b5`
- 資料庫：`Orion2.exe.i64` 一次性可寫副本
- 工具：IDA Pro 9.4、`tools/ida/audit_event_population_boom.py`
- 位址：IDA linear，DOS/4GW LE object #1；靜態證據，未做動態 oracle

## 已證實

1. `sub_2230A @ 0x2230A` 在 `0x22712..0x22790` 處理事件 17 適用性：呼叫
   `sub_23D44` 均勻抽一座正常殖民地；若抽不到、該殖民地已有互斥事件，或目前人口等於
   `sub_E0B4F` 算出的上限，候選直接失敗。這裡沒有二次抽選。
2. 建立端 `0x22BE5..0x22BF3` 把 age 設為 0，並把殖民地索引寫入事件 record `word+3`。
3. `sub_206A2` 的 case 17 `0x20EA0..0x20EC5` 只在 active status 2 呼叫
   `sub_E2A70` 重算殖民地，不直接增加人口。
4. 共用持續期尾端 `0x20B79..0x20BAC`、`0x2121D..0x21233`：age 0..4 不結束；age > 4
   時每回合擲 `Random(20)==1`；age > 20 強制進入結束狀態。建立當回合是公告期，下一回合
   才進 active status。
5. `sub_E1839 @ 0xE1839` 在 `0xE18B2..0xE18C3` 以 `sub_23509 @ 0x23509` 查詢
   事件 17 是否作用於該殖民地；若是，`0xE1C06..0xE1C0C` 對逐族成長百分點加 `100`，再由
   `0xE1CA4..0xE1CD9` 的既有成長公式消費。它不是固定 `+2` 人口，也不是最後結果乘二；是對
   既有加成總和再加 100 百分點。

## Remake 對映與限制

- 事件以穩定 `PlanetIndex` 保存目標，回合結算時只在輸入副本增加 `GrowthBonusSum += 100`；
  玩家、熱座與 AI 共用，不污染殖民地永久基礎值，且可 JSON 往返。
- 原版 `sub_23E60` 會拒絕同殖民地上的事件 2、14、16、17、24、25。remake 目前只有事件 17
  具行星級直接對映，因此已阻止重複人口暴增；其餘事件待各自重建 record 後再接完整互斥表。
- 原版殖民地易手時的 status 清理資料流未完成動態驗證；remake 目前以行星是否仍有殖民地判斷
  事件存續，所有權轉移時效果會跟著該行星的新持有者。此點標為近似。
