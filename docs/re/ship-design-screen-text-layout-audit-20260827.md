# 艦艇設計畫面文案與版面稽核（2026-08-27）

## 證據分級

- **已證實**：`sub_6C8F9`／`Add_Design_Buttons_ @ 0x69E62` 給出六個不等高艦體槽；底部 CLEAR／CANCEL／BUILD 座標為 `(374/461/547,443)`。
- **已證實**：武器成本／空間、微型化、改造、射界、彈架與多槽資料鏈已有 IDA／手冊證據，並被兩條戰鬥路徑消費；詳見 parity matrix 第 35 列、`weapon-mods.md`、`weapon-arcs.md` 與 `missile-ammo-rack-audit-20260824.md`。
- **已證實（remake source）**：畫面仍有 20 個 `tr`，另有 `>`、ADD／DEL、`S n/n`、BC／HP 等固定格式內嵌。多數 `extraText` 只有 `maxW`。
- **已證實（runtime 幾何）**：解鎖摘要折行數會動態推動空間表與建造錯誤，下游可進入 y=352 的改造區；這是 remake 排版缺陷，不是原版規則。
- **未知／adapter**：右上兩個原版資訊面板的完整逐字內容與原版操作控制流仍未閉合；現行多槽／改造控制是可玩 adapter。

## 本輪邊界

外部化固定玩家文案與控制 glyph，將動態資訊分配到固定安全框；不改武器規則、成本、空間、建造、射界、彈架或改造可用性。

## remake 執行期勘誤

- **已證實（runtime 幾何）**：中文字形墨水高度為 16px，舊改造晶片列距 15px 會讓相鄰列必然重疊。
  四列已改為 `y=366/382/398/414`、每列 16px，最後一列止於 `y=430`，仍在面板底界
  `y=431` 內。這是 remake 中文排版修正，不宣稱為原版座標證據。
