# 行星列表外部文案與版面規格

1. `planets()` 不得呼叫 `tr`；固定狀態與空清單提示由 `planet.list.*` JSON 鍵提供。
2. 無呼叫端的 `OutpostTargetHint` 必須移除，不保留規則層玩家句子。
3. 每個 50px 列的五個主欄與三個第二行欄位使用由欄邊界／列中心推導的 `textSafeRect`；不得只限寬。
4. 空清單與右側操作訊息各有獨立雙軸安全框；訊息不得侵入 y=386 的殖民船按鈕。
5. 雙語鍵、最長環境／狀態／行星名、文字框包含於列熱區及 `planets()` 無 `.tr(` 必須有測試。
