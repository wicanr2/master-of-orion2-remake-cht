# 誠實現況評估(moo2 remake)

> 起於 2026-07-04 使用者實測回饋「還原度約 20%」。**該 20% 是接原版 LBX 美術前的快照,已被 2026-07-05~12 多輪超越,勿再當現況錨點。** 本文保留核心紀律 + 現況 + 誠實 caveat;逐輪「怎麼接的」細節見 git log 與 `WORKLIST.md`,不在此重複佔篇幅。

## 核心紀律(此文存在的理由,rulebook/65)

還原度**只以「對原版實測比對」驗收,不看單元測試綠 / 新增系統數**。曾多輪用「加自製系統 + 測試綠 + headless 有畫面」謊報進度,但那些係數多是自編近似值(程式碼自標「remake 調校值」),測試只證自製邏輯自洽、不證對齊原版——使用者一玩就見差異。**還原 = 對齊原版,不是長出新東西。誠實標注每個數字來源,自編的就說是自編的。**

## 現況(2026-08-11)

可玩的多帝國 4X 迴圈(headless 探針驗:玩家 + 3 AI 各自擴張、經濟健康、可經三條勝利路徑贏一局、無 panic):

- **① 音樂 ✅**:PCM 場景 BGM(menu/galaxy/diplo/combat)+ 按鈕/戰鬥音效；`STREAM`／`STREAMHD` 與 `SOUND` 已完成 Docker 解碼、長度、峰值與非靜音技術抽樣，真正人耳聽感仍需外部音訊輸出環境。
- **② 忠實新遊戲流程 ✅**:版本選擇 1.3/1.5 → 難度/星系 → 14 族肖像選擇 → 自訂點數 → 命名旗色 → 真母星(人口職務模型**農4/工2/科2**,SAVE10 校準)。
- **④ gameplay 子系統全接**(非死碼):經濟(建築效果/重力/士氣/礦產/地形改造/污染/成長/收入/指揮點數)、科技抉擇樹、戰鬥(beam/飛彈/球狀分流 + 艦艇佔格 + 武器 mod)、地面戰(陸戰隊/戰車/軌道轟炸)、拓殖、多 AI(N=3,含 AI-vs-AI 關係矩陣)、間諜 STEAL/SABOTAGE/HIDE、領袖技能、三勝利路徑(征服/議會忠實搖擺票/安塔蘭反攻)。
- 飛彈改造的 ECCM/EMG/MV、魚雷 ENV/OVR/NR 與 ARM/FST 已接入設計、存檔、快速結算與格子戰術；PD 攔截餘數跨射擊／回合保存，ARM raw 對應仍標強推論，原版完整攔截器／AMR／戰機路徑不宣稱逐值相同。
- **客製種族特殊能力**：官方 22 項選項寫入 `CustomRaceTraits` 並保存；心靈感應、幸運、全知、匿蹤艦已接到外交、事件、能見度／偵測與戰鬥消費端。半機械族食物／生產半單位帳本已接入；食物複製機現在以半食物／半 BC 跨回合帳本處理，付款時機屬依 half-unit 存檔語意與手冊 1 BC/food 的**強推論**，仍未宣稱原版 runtime 逐值相同。
- **2026-08-10 收尾抽樣**：`NEWGAME.LBX` 資產數會決定 1.5 背景索引，舊 30 資產資料不再請求不存在的 `#31`；Docker 畫廊退出碼 0、35 張 PNG 全部產生，日誌無 LBX 越界／panic／fatal／error。
- **2026-08-11 remake 消費端收尾**：CMBTSHP 已在戰術移動後接固定 tick 的可重播短動畫；事件 8 已消費 `Random(201)+74`／20 點連鎖／下游 damage consumer 並把尺度化損傷寫入倖存艦；SABOTAGE 已追回 raw packed relationship byte、三段式 slot helper、兩張 score table 的讀取位置與門檻分支，並以結構化 AB/DB 分數、63 Agent 上限與 Spy-vs-Spy Agent 實際扣除完成 remake 近似；領袖 ETA `1→0` 已以 IDA 確認的 `sub_E2AB1`→`sub_E1D59`／`sub_DF8F0`／`sub_E2710` 鏈接上殖民地衍生欄位／全殖民地士氣刷新近似。原版 timer、score table 上游填值與 raw 設計／帝國欄位逐值 parity 仍明確標成 oracle 差異，詳見 [`docs/re/remake-consumer-closure-20260811.md`](re/remake-consumer-closure-20260811.md)。
- **經濟校準**(手冊考據 + SAVE10.GAM oracle + 探針):每科研 3、開局 BC 50、人頭基礎收入 1 BC/人、稅率預設 15、指揮基礎供給 5。2026-08-10 固定無事件 20 回合抽樣為 BC 50→264、人口 8→11、士氣 0% 全程、食物輸出 0→1、工業 6→8、研究 6 維持，沒有負食物或人口死亡螺旋；這是 headless 基線，不是玩家主觀聽感。詳見 `internal/shell/economy_20_turn_test.go` 與 `docs/knowledge-base/original-gameplay-reference.md` §7。

## 扎實資產層(有逆向驗證基礎)

LBX 解碼(scan-line RLE 影像 / 多幀 delta / 調色盤鏈)、SAVE10.GAM 唯讀解析、各原版畫面的真美術 + 擦底疊字、數百條 UI 譯表、go/ebiten + docker 編譯截圖 + AppImage 打包骨架。
(畫面數以程式碼為準:`grep -c '^func (b \*sceneBuilder) ' cmd/moo2/*.go`。文件裡寫死數字必然過期。)

## 仍近似 / 待校準 / oracle-gated(誠實 caveat)

- **③ 按鍵逐畫面像素對齊** ✅ **2026-08-07 走完**:座標改用執行檔反組譯的立即數。openorion2 的 `initWidgets` 只是次級來源、而且缺很多畫面——**openorion2 沒有 ≠ 沒有真值**。待重挖名單已清空,剩下的不是座標沒挖而是子系統沒做。
  ⚠ **2026-08-07 翻案**:這裡原本寫「無對應 openorion2 view 的畫面無硬編真值可覆蓋…要再精確只能靠原版截圖(本專案不採)」——**錯的**。真正的一手座標在**原版執行檔的反組譯**裡,與 openorion2 有沒有實作無關。當天做地面戰畫面(openorion2 對它零命中)時,反組譯直接給出全部座標:兩側面板貼圖點、`Darken_Fill_` 矩形、置中文字 x、列高、部隊落點公式與兩側基準 X。**openorion2 沒有的畫面,先去反組譯挖繪製呼叫的立即數,別退回估計值。** newgame 已於 2026-08-07 用這個方法重挖完(連帶修正「左下框是 PLAYERS 不是 RACE」的還原錯誤,
  並接上先前選不到的星系年齡與帝國總數,見 `docs/re/01-gap-report.md` 第 5 項(NEW GAME 設定畫面));colony 同日也做完(框架換成原版的 COLPUPS.LBX#5、職業欄用反組譯真值;
  colony 同日也做完(見第 6 項(殖民地畫面框架));  shipDesign 同日也做完(六個艦體槽在原版是**不等距**的,remake 先前照 17px 等距排;
  底部三鈕差 6–11px)。**這份名單清空了**——剩下的不是座標沒挖,而是子系統沒做
  (殖民地的行星表面 + 建築擺放、艦艇設計右上兩格的內容),見 gap report 末尾的進度表。
  - **行星表面的幾何已到手**(gap report 第 8 項(行星表面調查)):7×7 角點表從 `word_182C9C` 抽出、
    對 `COLONY.LBX#8` 的高亮菱形逐點驗過,走訪順序來自 `dword_BA784`;建築 sprite 是
    **已畫好位置**的 640×480 稀疏圖(資產 = 型別×36 + 格號),不需要座標就能貼。
    建築編號對照也拿到了:`Cache_Load_Bldg_` @ 0xAF6DC 給出 `資產 = ((id−1)%10)×36 + 格號`、
    檔案 `BLDG{(id−1)/10}.LBX`,編號本身由 openorion2 的 `BUILDING_*` 列舉與原版
    `TECHNAME.LBX` 第 295 條起的字串順序**互相對上**。離線合成 9 棟驗證:每棟都正落在
    自己的格子裡、遠近遮擋正確。落在 `cmd/moo2/colonysurface.go` + 9 條回歸測試。
    順帶推翻一句:行星表面**不是**關在中段那個框裡,它是整個 640×480 的底圖,面板疊在上面。
  - **行星表面 2026-08-07 收尾**(gap report 第 30、31 項):中段還給地表、建造佇列搬回原版的
    彈出視窗(`Build_Queue_Popup_`);原版 PRNG 照抄進 `internal/gamedata/origrand.go`,
    建築擺放換成 `Make_Bldg_Array_For_Colony_` 的原版演算法(蓄水池抽樣 + 分類排序);
    地表底圖接上(COLONY2.LBX#49 星空 + PLANETS.LBX 依 `氣候×3 + 變體` 選的地形),
    佔位格線移除。過程中抓到一個**不會有症狀**的軸向錯誤(格陣索引與角點表對調 → 整張
    佈局鏡射,徵狀只有「建築擠在遠端」),已加 `TestColonyGridKeyMatchesOriginalAddressing` 釘住。
    軌道衛星同日補上(`Draw_Colony_Satellites_`:x = 295 ± i×50、y = 162,圖來自 COLONY.LBX
    9/10/11/12/16 —— 編號要經過 `add edx, 9`,漏了會去讀零長度的資產 0..4,畫面空白且不報錯;
    抑制規則是原版的星基→戰鬥站→星際要塞升級鏈)。
    道路同日補上(gap report 第 14 項:7×7 格點、四方向、42+42+36+36 = COLROADS.LBX 的 156 張),
    房屋抖動與母星的國會大廈同日補上(第 14 項(地表道路)——Capitol「不在建造表裡」是對的,
    但它是實體建築,先前連地表也一起漏掉了)。
    植被層同日補上(第 14 項:13 組 × 8 張 = COLVEGGI.LBX 的 104,含「越遠越小」的透視項)。
    **建築集合已對齊**(2026-08-07 逐項了結,原本這裡寫「仍是缺口」是過期的):
    Colony Base 於第 17 項(拓殖基地)補上(`origColonyBaseID`);一次性改造(地形改造/Gaia/土壤改良/
    人造行星)於第 21 項(改造不佔格)**查證後確認不該佔格**——原版記旗標那一步對這四個編號是被跳過的,
    remake 天然正確,已加測試釘住。
    **真正仍缺的**:植被在「有格子被選取」時原版一株都不畫(remake 沒有這個狀態)、
    每株顏色未逐張比對;地形變體那一欄用 PRNG 代替原版的存檔欄位(不是真值,已標明)。
    ⚠ 「同一顆星會不會長一模一樣」需要對原版比對,而使用者 2026-08-07 指示逐畫面實測非必要
    ——所以這一項的驗收標準改成「亂數流與資料表逐項對得上反組譯」,已達成。
- **需原版 oracle 對照**(⚠ 2026-08-07 使用者指示:**逐畫面實測非必要**,真要驗用 **DOSBox**,不再用 archive.org 線上版;先窮盡靜態來源——執行檔反組譯立即數 > 手冊行文 > openorion2 原始碼。舊作法的產物 `docs/tech/oracle-comparison-20260712.md` 內容仍有效,但方法不再沿用):~~飛彈速度~~(**2026-08-07 移出**:`Missile_Speed_` @ 0x3CD21 一次解掉,不需要 oracle——手冊的「自相矛盾」是漏了條件,那個 +4 只在 Fast 改造的旗標 0x10 成立時才加;附表 = 一般飛彈、公式 = 有改造。**把它列進這一條是分類錯誤**,見 gap report 第 32 項(飛彈速度))、~~地面戰 d100 核心傷亡解算結構~~(**2026-08-07 移出**:`Ground_Combat_Round_` @ 0xEC4FE 給出原版的 26 位元組結構與完整回合,已照抄成 `gamedata/ground_battle_orig.go` 並接進入侵流程。與先前沿用的一代結構有三處實質差異,最要緊的是**平手時雙方都挨打**。逐類型的攻擊力/耐受調整也於第 33 項(地面戰結構)補上——手冊那句「Marine and Armor … your militia」把三種對上了:類型 0 = 裝甲(+10 攻擊 +1 耐受)、1 = 陸戰隊、2 = 民兵(−10),並訂正了 remake 把陸戰隊與戰車營排反的順序錯。民兵於第 33 項(地面戰結構)接上(`Colony_N_Militia_` @ 0xEC61E = ⌊人口/5⌋)。加成塊於第 33 項(地面戰結構)追完,並補上兩條手冊沒寫的規則:基礎耐受命中數是 1(某科技讓它變 2)、**難度加成不給人類玩家**(AI = 難度−2、安塔蘭側 = 難度×2−4,以「普通」為基準往兩邊偏)。**仍缺**:守方的裝甲那格留 0(AI 沒有建築追蹤機制,無資料可誠實推導 → 少算守方,方向上對玩家有利)、三張查表於第 35 項(三張查表)讀出來了(十二個科技 id 全部對上 remake 的列舉),抓到兩個實質缺口並補上:**鈦裝甲 +5**(手冊沒列,開局就有 → 每場地面戰都少)、**整條步槍通道**(上限差 30 點)。分兵種接線於第 33 項(地面戰結構)完成,並發現**手冊列的四個 hits 數字可以完全由反組譯的加法結構重建**(陸戰隊 1 / +動力裝甲 2 / 戰車營 2 / 機械戰士 3)——四個獨立的手冊數字由三個獨立的反組譯欄位加出來,是整個地面戰模型正確性的最強證據。仍缺:`[player+0x8A7]` 未定名、守方裝甲那格留 0。**部隊類型 3 已於 2026-08-08 第 46 項(叛亂)定名為叛軍**
  ——`Get_Rebellion_Info_` 把守方三種填進 `[+0x0A]`/`[+0x0C]`/`[+0x0E]`、叛軍數量填進 `[+0x10]`,
  同一陣列的第四格;`[+0x10]` 因此也一併有了意義)、~~安塔蘭母星防禦艦隊戰力(手冊/openorion2 均無精確數字)~~ **2026-08-08 第 49 項(安塔蘭防禦艦隊)解掉**
  ——`_n_max_antaran_def_ships`(`byte_181746`)逐位元組解出 `{0,0,3,2,7,0,…}`:
  **3 Large + 2 Huge + 7 Titan + 1 座星際要塞**,難度不改變組成。
  ⚠ 艦體尺寸對到 remake 六級戰力階梯那一層仍是推論、~~**母星開局態**(待校準)~~ **2026-08-08 複查:已結案**——`playerHomeworldColony`
  (`internal/shell/session.go`)現在就是 農4/工2/科2,與 2026-07-12 釘死的 oracle 一致,
  2026-08-06 那次校正已經把它改過來了。
- **建築表 41 棟全部有程式碼消費**(2026-08-07 第 41 項(戰機基地)重掃確認,0 棟純擺設)。
  ⚠ 這一列的歷史:先前寫「~13 棟需先建基礎設施」→ 第 38 項(行星護盾等三棟)改用程式碼掃描盤點得 11 棟 →
  第 41 項(戰機基地)發現**掃法本身有洞**(漏判「在 `buildings.go` 內宣告成常數、由別處引用」的建築,
  恆星轉換器就是誤判的那一棟),真正的缺口是 10 棟,第 38–41 項全部接完。
  ⚠⚠ **「有程式碼消費」不等於「完整還原」**,留白各自記在對應檔頭,不因為計數歸零而消失。
  2026-08-07 第 42 項(關掉兩條留白)回頭逐條檢查了一次,關掉兩條(**護盾的 Radiated→Barren 氣候轉換**、
  **未同化人口的 20% 多種族士氣懲罰**——後者連帶讓異族管理中心的第二條手冊效果生效)。
  2026-08-08 第 46 項(叛亂)再關掉一條(**未同化人口的叛亂**),第 52 項(生物武器分類)再一條
  (**屏障護盾擋生物武器**——擋門一直是「哪些科技算生物武器」這份名單,
  而它在執行檔的 category 表裡:category 20 恰好是 Bio-Terminator + Death Spores。
  分類是執行檔給的,不是自己劃的)。
  仍在的:戰機基地的 10 回合整補、護盾被炸掉後氣候不會變回 Radiated
  (**刻意的偏離**:remake 的建築效果沒有一個是可逆的,見 planetary_shield.go 檔頭)。
  **已接**:行星輻射/通量/屏障護盾(軌道轟炸每次攻擊 −5/−10/−20,手冊值,維護費三棟對上執行檔
  建築表)、自動實驗室(+30 研究點)、再生反應爐(每單位人口 +1 產能且不計污染——接在污染切分點之後)、
  食物複製機(饑荒時 2:1 換食物 + 1 BC/食物,**只補缺口**)、阿提米絲系統網(水雷:逐艦依艦體等級
  20–100% 觸發 × 8–28 枚 × 每枚 20 − 護盾等級;**2026-08-08 第 47 項(AI艦隊移動)起對 AI 艦隊也生效**
  ——AI 有星圖位置了,但因為它沒有逐艦資料,對 AI 是「艦隊戰力折損」的近似,不是逐艦模擬)、
  太空學院(第 39 項:艦員經驗系統整套接上——每回合 +1、同星系每座學院再 +1、
  該殖民地造的船起始等級 +1;BA/BD 加成真的進戰鬥了)、異族管理中心(第 40 項:征服人口同化,
  逐政體 2–20 回合、排斥種族 ×2、建築固定 2 回合不分政府。未同化人口的**兩個後果都已接上**:
  20% 多種族士氣懲罰(第 42 項(關掉兩條留白))與叛亂(第 46 項(叛亂))——同化因此不再只是計時器,
  是「征服打法要付的利息」)。
  ⚠ 這一列原本還寫著「戰機/航母(新戰鬥子模型)」——**過期,而且說得太重**。
  戰機**已經接進快速艦隊戰鬥**了(`gamedata.FighterBayCombatContribution` /
  `FighterHeavyBayCombatContribution`,中隊數 4 / 2 是手冊 GM p.127 硬數字,每架的攻/HP
  是標明過的 remake 近似值),`ResolveBattle` 與 `repair.go` 兩處都在用。
  戰術格子戰鬥裡的獨立戰機單位已接上(出擊、貼身攻擊、被敵艦射擊、返航、補給；突擊艇另走
  登艦路徑)，本輪再補上手冊 p.157 的「從目標最弱護盾分面攻擊」、help 第 402 筆的「戰機
  接戰前先開火 PD」，以及參戰種族 Ship Defense／Fighter Pilot 加成。仍缺 Helmsman 原版
  呼叫端、敵方逐艦戰機設計／中隊、原版艦身方向名稱與戰機精確武器命中／傷害 oracle；不把
  現有近似值寫成完整戰機還原。
- **起始科技等級(TECH LEVEL)**:2026-08-07 接上**兩個**真效果。
  ①**曲速前開局沒有 FTL**,艦隊離不開本星系,直到研究完核分裂(手冊直引,gap report 第 7 項(曲速前FTL限制))。
  ②**開局送的研究主題依等級而不同**(gap report 第 30 項(TECH LEVEL 第二效果)):那張表原本是「缺表不是缺工」,
  當天從 `Init_Player_Tech_` @ 0x5E55F 挖出來——數量 `var_18` = 1 / 6 / 25,
  清單 `word_18111C` = 29, 55, 22, 57, 28, 23(ENGINEERING / 核分裂 / 化學 / 物理 / 電子 / 冷聚變)。
  先前不論選哪一級都只發 2 個 = 等同曲速前,而選單上寫著「一般」。
  ③**開局建築照原版的優先清單生成**(gap report 第 31 項(開局建築清單)):清單 `word_17D8AC`(32 棟,
  最強的排最前面)+ 上限表 `byte_13A3A` = 3/5/9,兩張表都在 `Init_Homeworld_Colony2_` @ 0x13A3D。
  一般等級算出來仍是手冊講的那兩棟(截圖廊零差異)。
  **仍未接**:先進級在固定表之外還會隨機送的 19 個主題——要先港
  `Choose_Tech_Application_` @ 0xFD335(294 行的 AI 權重選擇器,吃成本表/種族旗標/性格/政府別)。
  ⚠ **2026-08-07 第 43/44 項解掉了**:19 個隨機主題照 `sub_FD335` 的結構發出去
  (唯一沒照抄的是 `sub_FC845` 那 985 行的逐科技估值,weight 一律當 1),
  開局建築那一層也改讀玩家實際知道的主題集合——先進級母星現在真的發滿 6 棟
  (曲速前/一般仍是兩棟,與手冊逐字相符)。
- **多人**:熱座(同機輪流)可玩；網路依原版決策保留決定性 lockstep、改用 TCP。最低可玩鏈已把
  主機大廳名冊接到共同新局、設定／種子／席位快照廣播、玩家操作指令同步、`turn_done` →
  `turn_ready` 兩階段回合結算與 `NetworkStateHash` 分歧停機接入 `cmd/moo2`；`networkWaitScreen`
  是正式對局入口，`netNextTurn` 只保留作畫廊示範。可選公網可靠性也已接上 resume token 重連、
  Ping/Pong 心跳／逾時／寬限、challenge-HMAC 身份驗證與 TLS 1.3，環境開關是 `MOO2_NET_AUTH`
  與 `MOO2_NET_TLS=1`。NAT 穿透仍需外部 relay 或 UPnP；原版的 IPX／數據機／序列埠／TEN 不恢復。
  熱座本身的兩個已知不對稱寫在 `docs/re/01-gap-report.md` 第 3 項。
- **AI-to-AI（可選）**:AI 選星的行星價值／距離模型、議會兩候選人與第三方搖擺票已在既有流程；
  `EnableAIVsAI` 開啟後，AI 彼此會依關係形成戰爭／停戰／互不侵犯／同盟、貿易／研究協議，
  並以抽象艦隊攻擊最高人口殖民地、結算佔領與持久化。這是 remake 模型，不是原版逐艦 blueprint；
  詳見 `docs/tech/ai-to-ai.md`。
  熱座本身的兩個已知不對稱寫在 `docs/re/01-gap-report.md` 第 3 項:非當前席位在 `EndTurn`
  最後才結算(差一個 AI 回合的資訊)、勝負判定只對當前席位跑。
- **UI 最小化**:議會 `COUNCIL.LBX#1` 的 10 幀與安塔蘭 `ANTAROOM.LBX#1` 的 55 幀已接入逐幀播放；accept/reject 仍以文字提示疊在原版底圖，非原版落敗對話框藝術與幀停留時間尚未取得 runtime oracle。間諜已在 RACES 內嵌區提供訓練與明確標籤的 STEAL/SABOTAGE/HIDE 切換，每個 AI 列也能進入外交對談。正式條約（和平／互不侵犯／同盟）、貿易／研究協議、固定 5%／10% 週期納貢、現金／科技／殖民地餽贈、食物／研究特殊貿易、玩家防守 Agent 與殖民地領袖任職均已接入狀態、回合收益／國庫轉移、終止、存檔與 UI。原版政府倍率與執行檔確認的神級商人 +50 個百分點也已接；remake 的結構化 SABOTAGE 分數／Agent 消費已接，raw slot helper 與 score 分支已由 IDA 對齊，仍未知的是兩張 score table 的上游填值、特殊槽位、特殊貿易 byte table、AI 接受門檻與防守策略。畫面雜湊、外部對照與低優先地面戰／事件／爆炸鏈留白見 `docs/re/visual-oracle-20260811.md`。
- **領袖技能**:2026-08-10 收尾盤點後,27 項中 **26 項至少有 remake 消費端**
  ——殖民地經濟 9 項、帝國／外交／間諜效果 8 項、艦艇／戰鬥效果 9 項。新增接線包含 Assassin
  逐位回合行動、Diplomat 的有標註關係代理值、Famous 雇用費、Megawealth 回合 BC／維護費、
  Operations 指揮點數、Spymaster／Telepath、Galactic Lore、Ordnance／Security 與 Fighter Pilot。
  過程中修掉三條一直做錯的規則:
  1. 手冊 p.137 說**只有 Megawealth 與 Researcher 可累加,其餘取最強的那一位**,
     而 remake 先前是每位都疊一份(第 45 項(領袖技能))。
  2. **HERODATA 的技能欄位是每技能 2 bit(= 技能階),remake 當成 1 bit 讀**——
     科學家/貿易家兩個標籤都貼錯人,進階技能的 +50% 一次都沒發生過,
     而且一位英雄只認得出一項技能(第 45 項(領袖技能))。
  3. **翻譯過的中文標籤被當成識別鍵**:英文模式下 `Skill` 存的是 "Scientist",
     三處查表全部落空 → **所有領袖加成同時靜默失效**;艦艇軍官的通稱「指揮官」又剛好是
     SKILL_COMMANDO 的譯名 → 每一位雇來的艦艇軍官都吃到地面戰加成(第 45 項(領袖技能))。
  ⚠ **戰術官不是缺口**:手冊那條的最後一句明寫「This skill is not implemented」,原版自己就沒做。
  Famous 的招募機率與 Diplomat 的獨立接受門檻仍沒有可證實的 runtime／資料模型；前者只接明確的
  雇用費折扣，後者映射到既有關係增益並明確標成代理值，不冒充原版逐值完成。
  > 「環保官沒有百分比入口」這個擋門理由於第 45 項(領袖技能)推翻——手冊那句
  > 「Reduces **the amount of production that causes pollution**」逐字就是
  > `PollutionPollutingProduction` 回傳的那個量,入口一直都在。
- **艦艇軍官管理（2026-08-09；2026-08-11 oracle 更新）**:原版 `save.Ship` 有逐艦 `Officer` 欄位，直接索引固定
  `_leaders[0..66]`；重製新增 `Ship.OfficerName` 與 `Ship.OfficerID`，並由 `HERODATA`
  帶入同一個來源序號。艦隊畫面選船後按 `LEADERS`，點艦艇軍官列即可指派／改派，再點同一列解除；
  `HIRE` 可進候選模式雇用指定傭兵，`POOL` 可解除指派回人才庫，`DISMISS` 可解雇艦艇軍官並清除
  逐艦欄位。Weaponry／Helmsman 命中與防禦、Navigator 航行、Engineer 戰後修復都只讀該艘船的指派。
  JSON 同時保存來源 ID 與名稱；舊存檔缺 `officer_id` 時以名稱回退。IDA 已證實原版
  `sub_10E2F @ 0x10E2F`／`sub_1160B @ 0x1160B` 對 `dword_1930DC` 直接讀寫 `0x3B×0x43`
  的全局領袖／軍官區塊；重製原生 `.GAM` importer 已接，`RawStatus=4` 的 `+0x37`／30 回合清理、`RawStatus=1` 的 ETA 遞減與活動 Trader raw 經驗 bucket 已接入；一般在職領袖 ETA 到期後的 `sub_E2AB1` 六槽 callback 與 `sub_E1D59`／`sub_DF8F0`／`sub_E2710` 下游已由 IDA 確認，remake 以領袖增量重整／全殖民地士氣刷新近似接入，raw 設計／帝國欄位逐值 parity 仍列 oracle 差異。
- **火線角戰術切片（2026-08-09）**:原版 `Relative_Bearing @ 0x32AD1`、
  `Relative_Bearing_XY @ 0x32A20`、`Move_Ship @ 0x3F5F1` 與 `Ship_Can_Deploy_At @ 0x49043`
  證實 16 向 heading、重疊 bearing mask 與左右 0／8 初始朝向；重製已把它接到格子戰術
  的玩家／敵方射擊與移動轉向，設計 arc raw 值仍保存並影響佔格／成本。原版快速結算的
  `QGet_Target`／`Strategic_Combat` 攻擊鏈未讀取 arc／bearing，重製因此維持抽象
  `battleVolley`；完整證據與限制見 `docs/re/weapon-arcs.md`。
- ~~**turn-1 校準開放項**:科學家分配(科3 vs 原版可能科4)待 playtest 定案~~
  > ⚠ **2026-08-08 複查:這一條已經過期了。** 程式碼裡是 `Farmers: 4, Workers: 2, Scientists: 2`,
  > 與釘死的 oracle 吻合;「科3」那個狀態在 2026-08-06 的校正就不存在了。
  > 這一條的存在本身是個教訓:**待辦事項會比它描述的問題活得更久**,
  > 所以斷言「某項還沒做」之前要先 grep(rule 63)。

## 中文化覆蓋率(2026-08-07 實測)

譯表 **5,049 條**(24 份 TSV),真正未翻 **1 條**——`BC`(貨幣縮寫,原版 UI 也是 BC,刻意保留)。
程式碼裡**沒有**會顯示到畫面上的寫死英文了——先前唯一那一處在 `-play` 簡約殼,
而那個殼 2026-08-08 已整個刪除(第 81 項(淘汰簡約殼))。主選單的語言切換 2026-08-07 補上
(先前只有啟動旗標 `-lang`)。

> ⚠ 這一節量的是**中文方向**(中文模式下還有沒有漏翻的英文),**不代表語言切換功能完整**。
> 反方向(英文模式下還有沒有露出來的中文)是另一回事,見下一節——**別把這兩個數字混為一談**。

量法——**只靠 `$1 !~ /^#/` 濾掉註解,不要另外加 `FNR>1` 跳表頭**:24 份裡有 23 份第一行是
`#` 註解(這條濾掉了),只有 `help.tsv` 第一行就是資料。多加一條跳首行會把那筆真資料吃掉,
數出來變 5,048。

```bash
# 譯表總條數
awk -F'\t' '$1 !~ /^#/ && $1 != ""' assets/i18n/*.tsv | wc -l
# 其中未翻的
awk -F'\t' '$1 !~ /^#/ && $1 != "" && ($2=="" || $2==$1)' assets/i18n/*.tsv | wc -l
```

## 英文模式覆蓋率(2026-08-10)

CLAUDE.md 明列「允許在主選單選擇中文/英文」。中文方向早就做完,英文方向這天才補——
在此之前 `-lang en` 跑起來,**多數 remake 自繪畫面仍然整片中文**。

**現況:`cmd/moo2` 的主要 UI 與已抽查的引擎產生字串已有英文模板。** 2026-08-09～10 已收掉畫廊中
可直接確認的資料顯示漏字：殖民地行星環境／特殊物產、建築與 Special action、歷史圖表指標及
AI 圖例、NEW GAME 五個值列、殖民地總覽／行星列表摘要、外交對手／選項、戰術敵艦／戰機與熱座席位名稱；
其餘引擎產生的敘述仍必須逐項分類，不能以一次 grep 或整批翻譯宣稱完成。

### 兩條路徑,只有一條天生雙語

| 路徑 | 做法 | 英文模式 |
|---|---|---|
| **overlay**(`overlayScreen` / `runOverlay`) | 擦掉原版美術上烘死的英文,疊中文 | **自動正確**——整段擦字疊字被 `lang == Traditional` 擋掉,原版英文直接露出來 |
| **自繪**(remake 自己畫的面板) | `fnt.Draw*` 直接畫程式裡的字串 | 先前**仍是中文**——底下沒有原版英文可露,字串本身寫死中文 |

overlay 那條的英文不是「翻譯」,是原版點陣美術本體,字型字距都是對的。

### 兩種修法,優先用第一種

- **讓路**:畫面底下是原版美術、英文烘在圖上時,英文模式整段跳過擦底疊字。畫面 100% 是
  1996 年的點陣字,比用 Noto 重畫更像原版,而且通常只要一個 `if`。
  用在:遊戲選單(GAME.LBX 六顆鈕)、多人設定(按鈕+標題帶)、種族選擇(14 顆族名鈕 +
  `SELECT RACE` 橫幅)、載入儲存(LOAD/CANCEL)、戰術戰鬥控制列(AUTO/SCAN/BOARD/…)。
- **`b.tr(zh, en)`**(`cmd/moo2/interactive.go`):底下沒有原版英文可露時才用。

### 順帶修掉的兩個中文模式看不出來的 bug

- 種族選擇的標題橫幅(RACESEL 資產 33)**從來沒被畫過**,只是被擦底的方塊蓋住。中文模式
  看不出來,英文模式一讓路就露出一片空白。
- `diplomatRaceIndex` 原本是手寫的 14-case switch,和 `raceSelectList` 是兩份會漂移的表;
  改成由後者推導(順序本來就一致),順帶能認英文族名。艦體名也一樣(原本三份對照表)。

### 剩下的:少量安全 fallback

`internal/` 仍包含資料表查詢 key、專有名詞與少量未分類的錯誤／開發訊息；這些不能在引擎層
直接全域替換成英文，否則會破壞查表或把未知語意誤翻。已抽查的主要自繪 UI、事件／發現／
突襲／安塔蘭／外交／戰術、客製種族、間諜／領袖與名稱池都有英文模板；未知自訂名稱、少量
錯誤原因與 dev-only 檢視器文字保留原值，避免把安全 fallback 冒充完整翻譯。

正解不是把 `tr()` 灑進引擎(引擎不該知道語言),而是讓引擎回**鍵值**、UI 端負責繪成文字;
或至少給那些資料表加 `En` 欄。殖民地與歷史圖表這輪採的就是這條路；無法安全反查的未知值
現在由 `cmd/moo2/englishlabels.go` 的 `englishSafeFallback` 保留 ASCII 自訂名或顯示通用英文，
不改查表 key，也不把 fallback 冒充完整翻譯。

2026-08-10 英文 `-gamegallery` 在 Docker + Xvfb 產生 35/35 張 1280×960 PNG，抽看主選單、
星圖、外交、艦艇設計與輸入框；日誌沒有資產越界、`panic`、`fatal` 或 `error`。英文棘輪測試
仍記錄 16 條可解釋的查表 key／dev-only／不可達分支，不再把它們當成玩家畫面漏譯。

### 怎麼量

`cmd/moo2/lang_gap_test.go` 是**棘輪**:用 go/ast 數「沒被 `tr()` 包住、又真的會畫到畫面上」
的中文字面值,超過 `langGapCeiling` 就 fail,只能往下調。

⚠ **別拿 `grep '\"...\"' | wc -l` 或 `tr(` 的數量當覆蓋率**:`tr("繼續", "CONTINUE")` 本身
就含中文字面值,補得越多 grep 的數字反而不動;而「讓路」那條路徑一個 `tr()` 都不需要
(`gamemenu.go` 就是 0 個 `tr()` 但英文模式完全正確)。

目前棘輪 **16**,而那 16 條全是偵測器分不出來的東西——查表 key(`shipClassZH`、
自訂種族的「政府型態」)、dev-only 單畫面檢視模式的視窗標題、以及英文模式跑不到的分支。
**真正的玩家畫面缺口是 0**；未知資料仍採安全 fallback，而不是聲稱所有內部字串都已有自然
英文翻譯。

驗收仍然是跑 `-lang en` 的過場截圖廊逐張看,不是看棘輪數字:

```bash
# 兩種語言各跑一次,逐張比對(中文模式應該逐像素不變,存檔時間戳除外)
/tmp/moo2 -game -data /data -font <ttc> -lang en -gamegallery <dir-en>
/tmp/moo2 -game -data /data -font <ttc>          -gamegallery <dir-zh>
```

`cmd/moo2/lang_coverage_test.go` 擋的是另一種漏:新增一族 / 一個旗色時漏填英文欄——
不會編譯失敗、中文模式也完全正常,只有英文模式會出現空白或中文。

### 兩個踩過的坑

1. **英文比中文長**:`hotseat.go` 那句英文說明第一版就溢出 320px 面板右緣。每加一段英文,
   要用 `-lang en` 跑截圖廊看實際排版,不要只看編譯過。
2. **`i18n.English` 是 `Lang` 的 Go 零值**(`English Lang = iota`)。`&sceneBuilder{session: s}`
   預設就是英文模式——production 端任何忘了設 `lang` 的建構路徑都會靜默變英文。
   `report_test.go` 因此加了明寫 lang 的規定與一條回歸測試。

## 鐵律

驗收看原版實測不看測試綠;不把「新增系統」當還原進度;誠實標數字來源;**還原度 % 待使用者對原版實測定案,不自評**。

## 2026-08-11 GAM／戰機／要塞狀態勘誤

- 原版 `.GAM` 已有可玩的 remake 匯入路徑：`ImportGAM`／`LoadGAMSession` 與
  `LoadSession` 的 `0xE0` magic 分流已完成；`SAVE10.GAM` 真檔抽樣讀入 36 星、82 行星、
  AI、領袖與玩家艦隊通過。研究完成 byte、special bit、任命／任期等尚未由同源資料流
  完整解出的欄位，匯入報告會明示，不冒充完整 parity。
- 敵方戰機現在使用 ID 31 第二組傷害範圍，並分開走 `sub_3AD57 @ 0x3AD57` 命中式與
  `sub_3AC20 @ 0x3AC20` Bomber 直接插值式，逐架進入最弱護盾／裝甲／結構消費；仍未知
  的是完整 raw record 逐彈 runtime 參數與 raw flags 命名。
- 星際要塞已把 `sub_4D18E @ 0x4D18E` 的四個已追回武器槽接到安塔蘭終局齊射；容量
  計算的 live tech 輸入與 raw bit 正式語意仍是未知，因此這是「已追回火力完整消費」，
  不是宣稱每個 runtime 數量都固定不變。

## 2026-08-11 Fire／要塞靜態公式勘誤

- IDA Pro 9.4 已追回 `sub_3AC20 @ 0x3AC20` 與 `sub_3AD57 @ 0x3AD57` 的相鄰 raw
  路徑；外部索引名稱衝突保留不解名。`sub_3AD57` 的 1..100 隨機、`<=95` 修正、
  40 命中門檻、`max-min+1` 插值、`DI==0` 半傷害與 `sub_39985`／`sub_3A0B9` 消費
  已證實；`sub_3AC20` 的直接 `S=max-min` 插值另由 `ResolveFighterBomb` 接入 Bomber。
  其 `var_18` 僅為 0／0x40，raw `&4` 分支不可達；remake 不再虛構該效果。
- 要塞 `sub_4D18E @ 0x4D18E` 的 class 6 table 直接 byte stride 讀址為
  `0x17F69C=900`；四槽 seed 為 `375/187/187/375`，raw 為 `2/0/4/2`，99／198
  是上限／拆槽邏輯。`sub_6EE8E @ 0x6EE8E` 的 divisor、raw 2/4 百分比與
  `sub_6E60E` base 已可重算；live tech `T` 與 raw bit 正式名稱仍是未知。
- 完整證據（輸入雜湊、工具、位址基準、原始名稱／運算元與推論等級）見
  [`docs/re/oracle-static-ida-20260811.md`](re/oracle-static-ida-20260811.md)。
