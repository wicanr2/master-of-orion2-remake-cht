# CONTEXT — 銀河霸主2 remake 的共用詞彙

本檔是這個 repo 的 domain glossary(rulebook/50)。**命名變數、寫文件、下結論時優先用這裡的詞**;
出現新概念先進這裡再用。重大決策不寫這裡,寫進 `docs/re/01-gap-report.md` 的編號項。

格式:`術語 — 一行定義。_避免_: 不要用的同義詞`

---

## 一、戰鬥的兩條路徑(最常被說錯的一組)

- **快速結算** — 一次算完整場艦隊戰的路徑(`battleVolley` / `combatant`),沒有格子、沒有真距離
  (射程一律當 2 格)，但仍以 `combatant` 保存彈藥、儲能等戰鬥回合狀態。星圖上碰到敵人時走這條。
  _避免_: 自動戰鬥、auto-resolve(專案內不用這兩個詞)
- **格子戰術** — 8×6 棋盤逐回合互動的路徑(`tacticalScreen` / `CombatShip`),有真距離、
  有移動預算、有跨回合狀態(充能/牽引/停滯)。
  _避免_: 戰術畫面(那指的是 UI,不是這條解算路徑)、tactical combat
- **兩條路徑** — 上面兩者的合稱。**任何戰鬥效果改動都要問「兩條都接了嗎」**——只接一條會安靜地
  不一致,這是本專案重複踩過的坑(第 68–71 項)。

## 二、造艦與元件

- **AI 殖民地產品** — 每座 AI 殖民地自己的目前建造項與進度，remake 以
  `AIOpponent.ColonyBuilds[star]` 保存。原版 `Colony_AI_` 也是逐殖民地指派；不可再把
  `TotalNetIndustry` 全部視為同一個造艦池，或讓同一份產能同時蓋建築又造艦。

- **元件(Component)** — 造艦畫面上可選的一項,分四張表:`WeaponOptions`(武器)、
  `ArmorOptions`(裝甲)、`ShieldOptions`(護盾)、`SpecialOptions`(特殊系統)。
- **元件表有 ≠ 效果有接** — 一項可以在表裡、花得了錢,而程式碼沒有任何地方讀它(第 72 項(元件表有≠效果有接)的
  隱形裝置)。**盤點時要分開問這兩件事。**
- **艦體等級(SizeClass)** — 巡防艦 0 … 末日之星 5,`gamedata.CombatShipClass`。
  _避免_: 艦艇尺寸、hull size(後者在手冊裡指佔格空間,不是這個)
- **引擎階(drive level)** — 1..6,對應核融合→相位引擎六個引擎科技。決定戰鬥速度與戰機速度。
  _避免_: FTL 等級、`ftlLevel`(舊參數名,語意其實是這個)
- **主動權(Initiative)** — `Beam Attack + 10×戰鬥速度`（手冊公式）。設定關閉時雙方分批行動；
  開啟時敵我艦艇合併後依分數降冪行動。同分保持建立序。

## 二之二、畫面與文字

- **邏輯座標** — 所有畫面版面用的座標系,固定 **640×480**(= 原版美術的原生解析)。
  熱區、面板、字級全部寫在這個尺度上。**加畫面時不要用螢幕尺寸算座標。**
- **hi-res 畫布 / `uiScale`** — 內部畫布相對邏輯座標的倍率,預設 2(→ 1280×960)。
  美術用 nearest 整數倍放大,文字用 2× 字級**在最終解析度重新柵格化**。
  _避免_: 全螢幕解析度、視窗大小(那些是使用者拉出來的,與畫布無關)
- **錄→放(record/replay)** — hi-res 的實作方式:文字繪製先被**記錄而不畫**,
  美術放大之後再以 2× 座標與字級重播(`internal/uifont/record.go`)。
  好處是**繪製呼叫端一行都不用改**。
- **z 序屏障(barrier)** — 錄→放的代價:錄下來的字最後才重播,所以「先畫字、
  再用面板蓋住字」會失效。對策是把**畫面板這個動作本身**當成屏障
  (`cmd/moo2/zorder.go` 的 `fillPanel` / `drawPanelImage`),畫之前先把已錄的字沖出去。
  **新增面板一律走那兩個函式**,不要直接呼叫 `vector.DrawFilledRect` / `dst.DrawImage`。
- **擦底疊字** — 把烘進原版美術的英文標籤蓋掉、再疊上中文(`labelRect`)。
  英文模式整段不畫,讓原版美術自己露出來。
  ⚠ 擦底板的座標**要在英文畫廊上量**,不是在中文截圖上目測。
- **艦艇設計容量摘要** — 只顯示目前選定艦體的已用／總空間與容量條；切換左側艦體時即時更新，
  武器改造保留在下方面板。這是 2026-08-27 採用的現代 UX 轉接，取代會跨越美術分隔框的六艦體
  常駐比較表。_避免_：在 640×480 主畫面同時塞入六列容量比較，或為此增加額外彈窗。

## 三、工作方法

- **正式網路回合等待** — `networkWaitScreen` 擁有唯一的 session poll 與兩階段鎖步 update loop，
  並共用 `netNextTurnScreen` 的原版面板 renderer、聊天記錄與輸入狀態；`netNextTurnDemo` 只是
  無 socket 畫廊資料。_避免_：把 renderer 稱為畫廊專用，或讓 renderer 再次 poll 而吞掉鎖步封包。

- **忠實化** — 把自編值/自編行為換成原版或手冊的真值。**與「新增系統」是兩件事**,
  不可混報為進度。
  _避免_: 優化、改善(那些不描述「對齊原版」這件事)
- **活表 / 唯一活來源** — `WORKLIST.md` 頂端的「剩餘工作」表(2026-08-08 起)。
  現況只以它為準;其他地方的清單一律視為可能過期。
  _避免_: 在別處複製一份剩餘工作清單(複製出來的每一份都會過期)
- **工程日誌** — `docs/re/01-gap-report.md`。**它不是現況**,裝的是 RE 硬資料(位址、資料表、公式)
  與逐項編號的日誌。**開頭那張「煉出來的規則」是唯一該讀完的部分**;底下的編號項是參考資料,
  要看某條規則的證據再去翻。查剩餘工作去活表。
  _避免_: 在別處複述它有幾項、幾條規則(那個數字每一輪都在長,複述出來就是下一個過期斷言)
- **擋門理由(blocking reason)** — 記錄「某項為什麼還沒做」的那句話。**會過期,而且沒有任何
  機制會提醒你**;第 52–72 項撞了八次。定期重問,不要相信註解。
  ⚠ 還有更糟的一種:**當初就寫錯**(第 71 項(探針③內部函式)的掃描距離、第 72 項(元件表有≠效果有接)的戰鬥艙)。
  「查不到所以標近似」是誠實的做法,前提是**真的查過**。
- **探針 ①②③④** — 四種找缺口的問法(見活表):①零覆蓋函式 ②零消費常數 ③被餵固定值的參數
  ④手冊有而 remake 沒抄的資料。
- **一手來源 / 二手推論** — 一手 = 原始檔案的內容(執行檔立即數、LBX 位元組、手冊行文);
  二手 = 檔名、美術字樣、命名習慣、目錄結構。**一手永遠贏二手。**
- **來源優先序** — 執行檔反組譯的立即數 > 手冊行文 > openorion2 原始碼 > LBX 資產尺寸交叉驗證
  > 量圖。靜態來源真的不夠才提 DOSBox 實測。
- **編譯器／runtime 排除 pattern** — 由位址與邊界、bytes／指令形狀、IDA library 辨識及
  caller role 共同錨定的編譯器生成 helper。DOS 的 Watcom stack probe／x87 初始化與 Win95 的
  Microsoft stack probe／C++／SEH 例外處理不算玩法缺口，也不照譯到 Go；分析時跨過 helper，
  繼續追玩家可見 consumer。_避免_：只憑函式名稱排除、把 middleware 整批當 runtime。

## 四、驗收

- **畫廊(過場截圖廊)** — `-gamegallery` 產生的 headless 截圖;`docs/screenshots/` 是基準。
  **逐位元比對是本專案的驗收測試**;`18_loadgame.png` 因含時間戳而不收進基準。
  基準是 **1280×960**(hi-res 畫布,見下);`-uiscale 1` 產出 640×480 的那一份。
  _避免_: 截圖測試(講不清楚是逐像素比對)
- **英文畫廊** — `-lang en` 跑的同一套。兩個用途:①看英文版面破版(中文截圖一張都看不到);
  ②**它是版面座標的一手來源**——擦底疊字在英文模式整段不畫,原版烘進美術的英文才量得到位置。
  _避免_: 用中文截圖去量原版英文標籤的位置(蓋著的東西量不準)
- **狀態指紋(StateFingerprint)** — 存檔快照 JSON 的 SHA-256 前 8 字,畫在 `30_netwait.png` 上。
  **指紋變了就代表持久化狀態變了**,必須查清楚原因,不可因為「改動有理」就放過。
- **棘輪(ratchet)** — `cmd/moo2/lang_gap_test.go` 的 `langGapCeiling`,只能往下調。
  ⚠ 它不是覆蓋率;覆蓋率要跑 `-lang en` 的畫廊逐張看。
- **測試綠 ≠ 對齊原版** — 單元測試只證自製邏輯自洽。這是本專案的第一紀律(rulebook/65)。
- **三項百分比儀表板** — 2026-08-25 起，README 可列有日期且可重算的三個工程比例：
  remake 功能完成度、原版玩法對齊度、發行驗證完成度。三者不可合成單一「還原度」，也不可
  推論逐幀或逐位元一致；分母、部分完成權重與來源必須緊鄰數字公開。2026-08-27 本週收尾
  快照為 **74%／49%／33%**；權威公式在 `README.md`，工作狀態在 `WORKLIST.md`，原版證據
  分級在 `docs/re/parity-matrix.tsv`。本週文案外部化仍屬既有進行中母項，所以分數沒有跨級。

## 五、路徑與環境

- **RE 工作區** — `/home/anr2/moo2-private-build/re`(`Orion2.exe.asm` 21 MB、`symbols_fixed.tsv`)。
  **有版權的遊戲檔一律不進 repo。**
- **VA→檔案位移** — 執行檔資料段的換算差值 `0x7E694`(第 65 項(種族特性31格)用約束搜尋定出來的)。
- 編譯與測試**一律走 docker**;Python 一律 docker uv.venv。

---

## Flagged ambiguities(待釐清,不要擅自收斂)

- **「還原度 %」** — 不再用一個數字回答。依 2026-08-25 決策改用三項工程儀表板；其中
  「原版玩法對齊度」只是 `parity-matrix.tsv` 的分級盤點快照，不代表玩家主觀感受、逐幀或
  逐位元等同。
- **Windows API／Win95 內部行為停止線** — 原版若把玩家可見功能交給 Windows API 或 Win95
  平台服務，只追呼叫前後的玩家可見契約；API 內部不列入 RE，remake 可採現代平台近似，並
  明標近似層級。_避免_: 為了重現作業系統內部實作而無限追查。
- **「戰鬥速度」** — 手冊同時用它指「格子上一回合能走幾格(原版 81 欄尺度)」與 remake 換算後的
  「1..3 格」。程式碼裡前者是 `CombatSpeed`,後者是 `TacticalMoveSquares` 的回傳值,別混用。
- **「掃描器」** — 手冊有兩種:掃描**科技**(空間/迅子/中子,帝國級,影響偵測距離與飛彈干擾抵銷)
  與**戰鬥掃描器系統**(逐艦元件,+50 光束命中 +2 parsec)。第 71 項(探針③內部函式)之前這兩個被混為一談。
- **「戰機庫 / 重戰機庫 / 轟炸機庫 / 突擊艇」** — 手冊 p.127 的四型戰機,`shell.FighterKind`
  四型都有(突擊艇隨登艦戰一起接上)。要說「有幾型」之前先看 `shell.FighterKind`。
